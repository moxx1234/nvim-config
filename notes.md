# The file contains random thoughts during everyday workflow that can improve it, but I don't want to waste time on it rn

LazyGit is becoming the Git CLI replacement in my workflow.
Sometimes git commands fail and I don't want to pay attention to some precommit warnings that can be fixed with llm in few minutes.
I can't select outputs from LazyGit command log for some reason, so I can't even paste it as text to Claude Code prompt
So I need a way to pass *reference to command log output* to the llm prompt.

Speaking about output logs:
I run some dev processes in tmux window and/or panes. Passing references to the tmux panes' content would also be really useful

